apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "site.fullname" . }}-test-connection"
  labels:
    {{- include "site.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "site.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
